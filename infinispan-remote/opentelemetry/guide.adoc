= OpenTelemetry Tracing
:summary: Enable OpenTelemetry tracing on an Infinispan cache and view traces in Jaeger
:mode: remote
:topics: observability,tracing
:keywords: opentelemetry,tracing,jaeger,observability,monitoring
:duration: 10
:source-dir: infinispan-remote/opentelemetry

== What You Will Learn

How to enable OpenTelemetry tracing on an Infinispan cache at runtime and send cache operations that produce traces viewable in Jaeger.

== Prerequisites

* Java 17+
* An Infinispan Server running with OpenTelemetry configured (use the provided `docker-compose` setup in this module, which starts both Infinispan and Jaeger)

== Step 1: Connect to Infinispan

Use the standard Hot Rod client to connect:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/opentelemetry/InfinispanRemoteOpenTelemetry.java[tag=connect]
----

== Step 2: Enable Tracing at Runtime

Enable tracing on the cache by updating its configuration attribute:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/opentelemetry/InfinispanRemoteOpenTelemetry.java[tag=enable-tracing]
----

This enables tracing dynamically without restarting the server.

== Step 3: Perform Cache Operations

Execute cache operations that will generate traces:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/opentelemetry/InfinispanRemoteOpenTelemetry.java[tag=put-ops]
----

== Step 4: View Traces in Jaeger

Open the Jaeger UI (typically at `http://localhost:16686`) and select the Infinispan service to view the traces generated by the cache operations.

== Step 5: Run the Tutorial

[source,bash]
----
mvn package exec:java
----

== What's Next

* link:/tutorials/remote-cache/[Get started] with basic remote cache operations
* link:/tutorials/remote-query/[Query your data] with Ickle queries
