= Remote Cache Aliases
:summary: Add aliases to remote caches and access the same data through different cache names
:mode: remote
:topics: administration
:keywords: cache,alias,remote,administration,hot-rod
:duration: 5
:source-dir: infinispan-remote/cache-alias

== What You Will Learn

How to add aliases to an existing remote cache so you can access the same cache data using alternative names.

== Prerequisites

* Java 17+
* An Infinispan Server running on `localhost:11222` (or Docker/Podman available for Testcontainers)

Start an Infinispan Server with Docker or Podman:

[source,shell]
----
docker run -it --rm -p 11222:11222 -e USER=admin -e PASS=password quay.io/infinispan/server:latest
----

TIP: You can replace `docker` with `podman` in the command above if you use Podman.

TIP: If no server is running, the tutorial code automatically starts an Infinispan Server using Testcontainers.

== Step 1: Connect and Store Data

Connect to the server, obtain a remote cache, and store a value:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/alias/InfinispanCacheAlias.java[tag=connect-and-store]
----

== Step 2: Add and Use Aliases

Use the admin API to update the `aliases` configuration attribute. You can then access the same cache data through any of its aliases:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/alias/InfinispanCacheAlias.java[tag=aliases]
----

The alias points to the same underlying cache, so all data is shared.

== Step 3: Run the Tutorial

[source,bash]
----
mvn package exec:java
----

You should see output like:

----
key = value
key = value
----

== What's Next

* link:/tutorials/remote-cache-admin-api/[Create caches] with the admin API
* link:/tutorials/remote-cache/[Get started] with basic cache operations
