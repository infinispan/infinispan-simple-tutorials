= Querying Data with Ickle and Protobuf
:summary: Index and query cached data using Ickle queries, Protobuf schemas, and full-text search
:mode: remote
:topics: querying,getting-started
:keywords: query,ickle,protobuf,protostream,indexed,full-text,search,projection
:duration: 15
:source-dir: infinispan-remote/query

TIP: The source code for this tutorial is available on link:https://github.com/infinispan/infinispan-simple-tutorials/tree/main/{source-dir}[GitHub].

== What You Will Learn

How to define Protobuf schemas with `@Proto` annotations, create indexed caches, and run Ickle queries including WHERE clauses, projections, and DELETE operations.

== Prerequisites

* Java 17+
* An Infinispan Server running on `localhost:11222` (or Docker/Podman available for Testcontainers)

== Step 1: Define Your Data Model with Protobuf Annotations

Infinispan uses Protobuf for serialization. Define your entities as Java records with `@Proto` and `@Indexed` annotations.
The `Person` entity uses `@Keyword` annotations for indexed fields, and the `PersonKey` record defines a composite key with `@Basic` projectable fields. A `@ProtoSchema` interface generates the schema for both:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/query/Person.java[tag=model]
----

== Step 2: Connect and Create an Indexed Cache

Register the Protobuf context initializer, configure the indexed cache with `indexing enabled="true"`, and connect:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/query/InfinispanRemoteQuery.java[tag=setup]
----

== Step 3: Run Ickle Queries

Query all entries, filter with parameters, use projections, and delete by query:

[source,java]
----
include::src/main/java/org/infinispan/tutorial/simple/remote/query/InfinispanRemoteQuery.java[tag=queries]
----

== Step 4: Run the Tutorial

[source,bash]
----
mvn package exec:java
----

== What's Next

* Try link:/tutorials/remote-spatial-queries/[spatial queries] for geolocation data
* Add link:/tutorials/remote-continuous-query/[continuous queries] for real-time notifications
